# Prompt for Codex

Open the existing `abdullahahsen05/plandelta-ai` repository and extract this execution pack into its
root. Read `AGENTS.md`, `START_AGENTIC_V0_2.md`, `PLAN_AGENTIC_V0_2.md`, `PHASES_AGENTIC_V0_2.md`,
every `docs/*_V0_2.md` file, and the existing repository documentation before changing code.

Preserve the complete v0.1.0 history and tag. Verify the actual baseline and credentials safely,
then create or resume `feat/agentic-v0.2` and execute Phase 12 through Phase 22 sequentially. Keep
working continuously into the next phase without asking for routine approval or stopping after
commits, pushes, partial features, successful focused tests, local completion, or deployment. Stop
only when the complete definition of done passes, except for a genuine external blocker that
prevents all remaining safe work.

Keep `PHASES_AGENTIC_V0_2.md` current. Make small meaningful Conventional Commits after each
coherent unit of progress and at least once per phase; use multiple reviewable commits for large
phases and never accumulate huge chunks of unrelated code or multiple phases in one commit. Push
verified checkpoints and immediately continue. Never fabricate results.

Build the Evidence Copilot, local-embedding hybrid RAG in Supabase pgvector, bounded load-bearing
multi-agent graph, verifier/citation system, durable chat state, interactive citations,
engineering-schematic profile/sample, guardrails, traces, evaluations, and cost-controlled
deployment exactly within the pack's boundaries. Do not add a generic chatbot, arbitrary-image
claims, new paid vendors, prohibited AWS services, or external side effects.

During implementation use focused checks. After all features are integrated, run the entire
`docs/FINAL_VERIFICATION_V0_2.md` matrix. For every failure, diagnose and fix the underlying issue,
rerun the smallest failing check, then restart the complete final matrix. Continue until all
required checks pass together or a genuine external blocker is documented. Do not skip, weaken, or
delete tests to obtain a green result.

After local and CI gates pass, deploy within the existing AWS/Vercel budget, verify the production
journeys, populate the application evidence document from real source/tests/history, merge the
reviewed branch, and create `v0.2.0` only when every release gate is true.
